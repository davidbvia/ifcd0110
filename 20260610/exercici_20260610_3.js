let amplada, alçada;

function area(amplada, alçada) {
    // Calcular l'àrea d'un triangle
    let area = (amplada * alçada) / 2
    return area;
}

console.log(`L'àrea del triangle és: ${area(5, 3)}`);